# `@vel/privacy-gateway`

Pre-model privacy gateway scaffold. This is intentionally not implemented first.

Critical rule: privacy redaction must happen before raw content reaches any LLM. A future privacy MCP can expose safe stats/review actions only; it must not expose rehydration maps.
