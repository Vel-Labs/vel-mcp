# `@vel/mcp-base`

Thin MCP adapter package. Capability modules should use this package instead of importing MCP SDK packages directly.

This is important because the MCP TypeScript SDK is evolving. The scaffold pins production packages to the stable v1 SDK path while keeping migration work localized.
