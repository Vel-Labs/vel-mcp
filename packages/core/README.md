# `@vel/core`

Shared runtime primitives for VEL packages. No MCP SDK imports and no specialist model dependencies belong here.

Primary responsibilities:

- config loading
- artifact store
- audit hash chain
- lazy worker supervision
- provider registry
- path policy

See `ROADMAP.md` and `docs/package-roadmaps/core.md`.
